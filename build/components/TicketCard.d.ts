/// <reference types="react" />
import { Ticket } from "../";
interface TicketCardProps {
    ticket: Ticket;
}
export declare const TicketCard: ({ ticket: ticket }: TicketCardProps) => JSX.Element;
export {};
